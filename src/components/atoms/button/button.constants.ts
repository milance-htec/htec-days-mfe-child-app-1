import { ButtonTypes } from './button.types';

export const BUTTON_TYPES = {
  PRIMARY: 'primary' as ButtonTypes,
  SECONDARY: 'secondary' as ButtonTypes,
  GHOST: 'ghost' as ButtonTypes,
  TEXT: 'text' as ButtonTypes,
  LINK: 'link' as ButtonTypes,
};

export const BUTTON_TESTID = 'button';
